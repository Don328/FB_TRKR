namespace FBTracker.Server.Data.Schema.Constants;

internal static class ParameterNames
{
    internal const string id = 
        "@" + PropertyNames.id;

    internal const string season = 
        "@" + PropertyNames.season;
    
    internal const string name = 
        "@" + PropertyNames.name;
    
    internal const string locale = 
        "@" + PropertyNames.locale;
    
    internal const string abrev = 
        "@" + PropertyNames.abrev;
    
    internal const string conference = 
        "@" + PropertyNames.conference;
    
    internal const string region = 
        "@" + PropertyNames.region;
    
    internal const string teamsConfirmed = 
        "@" + PropertyNames.teamsConfirmed;
    
    internal const string schedulesLoaded = 
        "@" + PropertyNames.schedulesLoaded;

    internal const string week =
        "@" + PropertyNames.week;

    internal const string homeTeamId =
        "@" + PropertyNames.homeTeamId;

    internal const string awayTeamId =
        "@" + PropertyNames.awayTeamId;

    internal const string byeTeamId =
        "@" + PropertyNames.byeTeamId;

    internal const string dayOfWeekIdx =
        "@" + PropertyNames.dayOfWeekIdx;
}
